-- User init.lua
-- Loaded in Phase 4 (behavior) after global layer so user can override
-- options, keymaps, autocmds, and LSP setup. Return nothing; just run code.

-- Example: change colorscheme and some options
-- vim.o.number = true
-- vim.cmd([[colorscheme gruvbox]])

-- Put arbitrary user bootstrap here.

