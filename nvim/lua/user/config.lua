-- User-specific configuration
-- Put any overrides here. This file is loaded in Phase 1 (config) after
-- the global/corp/site/team/project config files, so it can override
-- any global defaults.
--
-- To completely replace the plugin set (i.e., use only your own plugins
-- and skip the bundled global plugins), set the following to true:
--   vim.g.loadout_user_plugins_only = true
-- Then create lua/user/plugins.lua (or lua/user/plugins/*.lua) to define
-- your plugin specs for lazy.nvim. See lua/global/plugins/* for examples.

-- Example (uncomment to enable):
-- vim.g.loadout_user_plugins_only = true

-- Other example overrides:
-- vim.g.cfg_dpc = true         -- disable plugin update checker notifications
-- vim.g.have_nerd_font = true  -- tell the config a nerd font is available

return {}
